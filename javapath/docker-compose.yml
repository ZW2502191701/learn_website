services:
  javapath:
    build: .
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production

  # Future: Spring Boot backend
  # backend:
  #   build: ../backend
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     - SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/javapath
  #     - SPRING_REDIS_HOST=redis
  #   depends_on:
  #     - db
  #     - redis

  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     - POSTGRES_DB=javapath
  #     - POSTGRES_USER=javapath
  #     - POSTGRES_PASSWORD=changeme
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data

  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"

# volumes:
#   pgdata:
